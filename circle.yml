machine:
  node:
    version: 8.0.0
dependencies:
  override:
    - yarn
  cache_directories:
    - ~/.cache/yarn
test:
  override:
    - yarn test
deployment:
  production:
    branch: master
    commands:
      - yarn build/production
      - yarn upload/production
      - yarn updateCloudfront